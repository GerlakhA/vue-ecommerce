export interface ISneakers {
	id: string
	title: string
	price: number
	imageUrl: string
	isFavotite: boolean
}

export interface ISneakerById {
	id: string
	title: string
	price: number
	imageUrl: string
	isFavorite: boolean
}
